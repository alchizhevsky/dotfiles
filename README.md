# dotfiles

## manual actions after cloning
-  link global git config
```
ln -s ~/.config/git/config ~/.gitconfig
```

## TODO
- gitignore generated configs (like fish/completions/*)
- edit .gitignore to include some directories fully
